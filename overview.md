# 泉州游金麻将 · 阶段交付（v3 = 单机可玩版）

> **历史文档（2026-08-03）**：以下为单机可玩版阶段交付报告。现役状态以 **AGENTS.md** 为准——好友房联机与实时语音均已上线（2026-08-11/08-13），测试 51/51。本文件保留历史叙事，不再更新。

## 本轮新增（v3）

### 单机可玩版
- **Vite + React + TS** 工程（`npm run dev / build / test`，构建产物 ~56KB gzip）。
- 5 个页面（hash 路由 + `?r=` 查询参数双轨）：首页 / `?r=pregame`（选 4 局或 8 局）/ `?r=game`（牌桌）/ 规则 / 设置 / 战绩（最近 20 场）。
- 牌桌界面：四方位、图案化牌面（v3 样式）、金牌指示、罗盘、操作按钮、计时、事件日志；点击手牌出牌，响应窗口自动弹出吃碰杠胡过。
- 单机零后端依赖，部署到任意静态平台（EdgeOne Pages 即可）随时可玩。

### AI
- `src/game/ai.ts`：含金向听数评估（回溯含金的 S/P）+ 吃碰杠胡过/单游宣告决策。
- 决策优先级：**游金宣告 > 胡**（+9/+18 期望高于 +6，规则本意即此）+ **拒胡救援**（吃进这张牌恰好构成单游牌型则拒点炮胡、等自摸宣告）+ 轻量进取（15% 概率放弃点炮等自摸）。
- `src/game/controller.ts`：会话状态机驱动引擎 + AI + 真人，自动超时、本地战绩保存。

### 校准基线（AI 自对弈 40 场 × 4 局 = 160 局）
- 对称性：四家胡局分布 30.3 / 23.0 / 25.0 / 21.7%（容差 ±15% 内 ✓），积分对称、零和守恒。
- 流局率 ~5%，单游 AI 自对弈 ≈ 0.4%（AI 稳健打法下罕见——单游本质是玩家主动冒险策略的奖励；界面已提供宣告入口）。
- `tests/ai-sim.test.ts` 持续跑回归；真人长期胜率 50% 由试玩校准（`aiLevel=0.9`、`greed=0.15` 旋钮可调）。

## 累计成果

- **规则引擎**（`src/engine/`）：144 牌建模、发牌补花、开金、含百搭胡牌判定、吃碰杠、响应裁决、单/双/三游、流局、连庄、整场推进、固定积分零和。
- **验收测试**：46/46 全绿（3 个引擎测试文件 41 项 + AI 自对弈模拟 5 项，含零和、对称性、单游频率、AI 合法性）。
- **文档**：`docs/rules.md`（规则 + D1–D6 + 平衡路径）、`docs/deploy.md`（EdgeOne Pages + CloudBase 部署）。
- **视觉稿**：首页 / 好友房大厅 / 牌桌 三张高保真稿（812×375，图案化牌面 v3 样式），各附 1:1 截图。

## 下一步

1. 真人试玩微调 AI（`aiLevel` / `greed` 旋钮）；如需单游 AI 可见性显著提升，加"做牌目标导向"逻辑。
2. CloudBase 好友房（7 个接口：createRoom / joinRoom / setReady / startMatch / submitAction / resumeSeat / leaveRoom）。
3. 方言语音（需泉州话验听）、全设备横屏测试、按 deploy.md 上线。